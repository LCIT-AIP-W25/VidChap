Turned the whole backend function into an api using fast-api library. The current version works with no breaks.
The api takes in a youtube links as input and after processing gives back 2 variables containing the transcript for the video along with the chapeter name and boundarie.
